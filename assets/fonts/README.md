The `x360` font file is present at `assets/X360.ttf` in this project.

`css/x360.css` is configured to load `../assets/X360.ttf` (case-sensitive on some servers).

Only `index.html`, `fail.html`, and `off.html` include `css/x360.css`, so the font will apply only on those pages.